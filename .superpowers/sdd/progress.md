# SDD Progress Ledger — M9/M10/M11 (FORGE_PROPOSAL.md lines 1107–1267)

Session start: 2026-07-14. Branch: feat/r5-cost-model.

## Standing facts
- Desk is LIVE: heartbeat sync commits ledger/ + state/ every 5 min on this branch. NEVER `git add -A`; stage code files explicitly.
- Working tree at session start held ~1,600 uncommitted lines of prior-session M9/M10 work (dossier.py, labeling.py, reflection_client.py, reflection.py rewrite, specs.py challenger, head_of_desk.py, web/app.py endpoints).
- Decision: work in the MAIN tree, sequential tasks (milestones are strictly dependent M9→M10→M11; uncommitted work would be invisible to worktrees).
- Test command: C:/ProgramData/Anaconda3/python.exe -m pytest tests/ -q --ignore=tests/test_forge_agent_timeout.py --ignore=tests/test_forge_heartbeat_schedule.py
- Baseline (session start, related subset): 119 passed, 13 failed (6 in test_m9_modules.py evaluator/controller, 7 in test_reflection.py).

## Plan decisions (pre-flight review)
- Gap analysis: scratchpad/gap-analysis.md (session cf81ee68). M9: 3 DONE/6 PARTIAL. M10: 1 DONE/5 PARTIAL/1 MISSING. M11: all 6 MISSING.
- Stale hygiene test vs deliberate commit fce2df3 (amber_wolf/gray_finch re-tracked as seed theses): update the test's orphan list. Documented, not escalated — later commit is explicit and deliberate.
- M9 test table names `test_spawn_from_harvest` but spawn-from-seed is M11 crit 5: deferred to T11.
- llm_fn contract standardized as `llm_fn(system_prompt, user_prompt) -> str` (reflection_client.complete shape).
- Reviewer diffs generated with `:(exclude)ledger` `:(exclude)state` — live heartbeat commits interleave on this branch.
- All subagents: Sonnet (user guideline). Implementers must stage explicit paths only (never `git add -A`).

## Blocker log
- 2026-07-15 ~01:05Z: subagent sessions hit the account session limit (resets 10pm PT / 05:00Z). t4-review and t4b-impl died mid-flight (no commits, no reports). User said "continue" → orchestrator continues INLINE (implements + reviews itself). Resume subagent dispatch if spawns succeed later.
- 2026-07-16 ~04:51Z: hit the account session limit again — this time the orchestrator itself (forge-orchestrator, Fable) went idle/failed, not just a dispatched implementer ("Implement T8 challenger regret" implementer also failed mid-flight, no commit). Reset reported as 1:50am America/Los_Angeles (~08:50Z, ~4h out from 04:51Z). T7 is the last confirmed-clean state (commit aa9cd2d). T8 had NOT started any commit before the failure. Parent session nudged the orchestrator to resume; awaiting reset.
- 2026-07-15 (new session): found HEAD (328dc44) inconsistent — e99704d/5c00f92 (T6 impl) already reference/describe store/specs.py's deploy_as_challenger/get_challenger_spec/resolve_challenger and decision_loop.py's shadow-challenger block, but those two files were never staged (T6's commit missed them — explicit-path staging discipline gap). Working tree still had the code from the interrupted prior session. Verified full suite green (660 passed, 1 skipped, 0 failed) with the code included, then committed as d571910. T6 implementation is now actually complete and consistent; T6 code review (spec compliance + quality) has NOT yet run — do not skip it.

## Tasks
- [x] T1 Reflection transport contract (M9 c1+c2): complete (commits 055e373 + fix 6b10e3b, review clean after 1 fix loop; 41 tests in scope passing)
- [x] T2 Evaluator/controller tests (M9 c4/5/6): complete (commit 141de6f, review clean; full suite 616 passed 0 failed). Note: T2's commit carries the pre-existing TestHeadOfDesk rewrite in test_m9_modules.py (tests still-uncommitted meta/head_of_desk.py — T5 commits that module).
- [x] T3 Trigger-all endpoints (M9 c3): complete (commits 8591e4e + fix 4463e9e, re-review clean). Included escalated fix: benchmark agents can never reflect (guards in check_agent_eligible + run_reflection Gate 0; all three trigger paths verified blocked). Suite 622 passed.
- [x] T4 Risk officer (M9 c7): complete (commits 9b64016 impl + b14ec2b review fixes + 5e7baba T4b enforcement). Orchestrator's inline review found+fixed: (1) blackout/transient closures were materialized into permanent entry_disables rows (would freeze desk after first FOMC window); (2) throttle never re-enabled (reconciliation added); (3) fail-loud desk config; (4) T4b: entry gate now ENFORCED in decision loop via entry_gate_status() — closes never blocked; legacy per-agent checks made default-OFF (invented $1k/$100 defaults would have frozen agents once enforced). Suite 640 passed. NOTE for final review: risk_officer reads drawdown_kill_pct/max_position_size/agent_daily_loss from config TOP level (predates desk.* convention) — desk.drawdown_kill_pct is NOT what it reads; harmless now (defaults off) but inconsistent.
- [x] T5 Head of Desk (M9 c8): complete (commit 085aac3, inline by orchestrator). Briefing cron 06:00 UTC + overview panel + regime-memo section; LLM-composed chat w/ fallback; streaming WS (chunk+done protocol); chat_history persisted+rendered. Suite 645 passed.
- === M9 COMPLETE (commits 055e373..085aac3): crits 1-8 shipped+tested; crit 9's /decisions surface rides with T7 (proposal: T7 generalizes that exact page to labeling coverage). Briefings persist to dedicated `briefings` table, not `evaluations` (criterion text says evaluations; schema FK makes that a bad fit — divergence documented for final review). ===
- [x] T6 Reflection gates + atomic deploy (M10 c3+c4, + swept-in labeling/dossier from prior session): complete (commits e99704d + 5c00f92 + d571910 + review fixes 9b6d0e8; review Approved after 1 fix loop). Review range 085aac3..d571910 covered M10 c1-c4 + challenger deploy mechanics. Fixes: (1) CRITICAL close-decision labels correlated wrong trade — now via trade_id from fill; (2) shadow-challenger safety invariant test added (tests/test_challenger.py); (3) resolve_challenger scoped to trial window (>= deployed_at); (4) staleness bound on _fwd_return_at_cutoff (10min, gaps → null). Judgment calls (20% fragility rule; secondary_evidence counted in term budget) reviewed OK. Suite 673 passed, 1 skipped, 0 failed.
- [x] T7 Labeling wiring + dossier integration (M10 c1+c2): complete (commit aa9cd2d, review Approved, no Critical/Important). Most of c1+c2 had shipped inside T6's commits; T7 closed the residual gaps: M6 counterfactual filler absorbed into the nightly labeling job (standalone "counterfactual" scheduler job removed; run_labeling_job calls the unmodified run_counterfactual_replay so counterfactual_* semantics identical), and /decisions unified to ONE "Decision Coverage" surface (forward-labeling headline, counterfactual fill stats nested under coverage["counterfactual"]). Suite 678 passed, 1 skipped, 0 failed.
- [~] T8 Challenger regret + hypotheses (M10 c5+c6): implementation committed (ea09c5e resolve_challenger→regret, 69496db hypothesis registry lifecycle, db9c1a4 resolution job scheduled, df930df dossier/agent-page hypothesis track record). Full suite green (693 passed, 1 skipped, 0 failed). CORRECTION (2026-07-16 ~09:00Z): the T8 REVIEW completed and its findings are PRESERVED in the orchestrator context + below; only the FIX subagent died on the session limit (before any edit — tree verified clean). Review verdict was Needs fixes: core regret rewrite Approved-quality (production-shaped fixtures verified against agents/decision_loop.py log shapes; trial-window + zero-evidence guards solid), but 2 Important: (F1) web trigger endpoints (/api/exec/trigger-reflection, /api/exec/trigger-all-reflections) never create a reflections row → hypotheses get reflection_id NULL and forge's resolution job filters reflection_id IS NOT NULL → stuck in 'challenger' forever; (F2) max_days expiry with zero labels marks hypotheses terminally 'inconclusive' while leaving the challenger spec row live → if labels arrive later and trial resolves, hypotheses never corrected (resolve_hypotheses only touches proposed/challenger) — fix by making expiry terminal for BOTH trial and hypotheses, and pre-expiry not_resolvable must touch NEITHER. Plus Minor: (F3) check_challenger_resolution's labeled count lacks regret_pct IS NOT NULL filter that resolve_challenger applies. Also minor: _insert_labeled_decision helper duplicated across test_challenger.py/test_hypotheses.py; badge-{{status}} CSS classes may be unstyled. Judgment calls reviewed OK: canonical 4h horizon; cycle-level validated/falsified mapping; hourly cadence. NOTE: reviewer confirmed there is NO decisions.confidence column (the earlier ledger line saying otherwise was wrong) — regret sourcing from decision_labels supersedes the whole confidence question. Fix round 1 (commit c730a0a, suite 700/1/0): F1 web endpoints now route through run_reflection_cycle (single reflections INSERT site); F2 expiry now terminal (spec→'inactive', verdict "expired_no_signal") and pre-expiry not_resolvable touches nothing; F3 regret_pct IS NOT NULL parity. From-scratch r2 review (range aa9cd2d..c730a0a) verified F1-F3 genuinely fixed but found NEW: (rA, CRITICAL) challenger shadow decisions are never labelable in production — decision_loop's shadow rows lack the "order"/"candidate" keys meta/labeling.py's extractors require, so decision_labels never gets challenger rows → every live trial ends not_resolvable/expired_no_signal, promotion impossible (tests passed via direct label inserts bypassing the production path); (rB, Important) forge.py's reflections.outcome UPDATE sits inside the per-hypothesis loop → zero-hypothesis cycles show PENDING forever. Fix round 2 dispatched for rA+rB with a mandatory end-to-end production-path test. r2 minors deferred: _insert_labeled_decision helper duplicated across two test files; deployed_at lookup query duplicated between check_challenger_resolution and resolve_challenger; hypotheses of never-deployed (gate-rejected) proposals stay 'proposed' forever (spec-consistent, confirm intended at final review).
- [ ] T9 Trial accounting + bootstrap null (M11 c3+c4)
- [ ] T10 Desk memory + graveyard extension (M11 c1+c2)
- [ ] T11 Population operators (M11 c5)
- [ ] T12 Desk scoreboard (M11 c6) → COMMIT M11 → final whole-branch review

## Minor findings deferred to final review
- (T1 review) tests/test_reflection.py::test_no_previous_spec_still_works: docstring/comments now inaccurate (pipeline change moved it onto the parse-failure branch); passes only via lenient `deployed is True or rejection_reason is not None` OR-assertion. Fold into T6 (rewrites this file) — tighten assertion + fix docstring, and clean the desk_config→desk comments in test_min_trade_gate/test_update_throttle.
- (T1 impl concern) llm/reflection_client.py reads data/forge.db (live DB) directly when not monkeypatched — future tests of that module must monkeypatch; consider injectable conn later.
- (T6 review, minor) agents/reflection.py walk-forward call reads `config.get("taker_fee", 0.00035)` at TOP level but taker_fee lives under desk. — silently always uses default (coincidentally equal today).
- (T6 review, minor) store/specs.py deploy_as_challenger rollback path unlinks the thesis file but orphans the spec YAML file on DB-insert failure.
- (T6 review, minor) web/templates/decisions.html renders literal "&mdash;" inside a {{ }} expression (autoescaped) — cosmetic.
- (T7 review, minor) run_counterfactual_replay call inside run_labeling_job is unguarded — a query-level exception there makes forge's log read "Forward labeling job failed" even when labeling itself succeeded (per-decision commits mean no data loss).
- (T7 review, minor) duplicated candle-read helpers between meta/labeling.py and store/counterfactuals.py remain (brief-permitted debt).
- (T6 re-review, FOR T8 — must fix) resolve_challenger's incumbent baseline reads details.get("confidence") from decision_details_json, but production incumbent enter rows log {"order","fill"} with NO confidence key (confidence lives in the decisions.confidence COLUMN) — on live data incumbent avg is always 0.0 so any challenger auto-promotes. TestResolveChallenger fixtures insert a {"confidence":...} JSON shape production never writes. T8's regret rewire must fix the sourcing AND update fixtures to the real shape.
